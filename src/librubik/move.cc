#include "move.hh"

#include <vector>

namespace rubik
{
    std::vector<Move> parse_moves(std::string input)
    {
        input = input;
        return std::vector<Move>();
    }

} // namespace rubik
